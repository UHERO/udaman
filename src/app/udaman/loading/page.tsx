import { Loader } from "@/components/ui/loader";

/** Page exists just to demo loaders :) */
export default function LoadingPage() {
  return (
    <div className="flex justify-center pt-[33vh]">
      <Loader />
    </div>
  );
}
