/**
 * @module timeseries
 *
 * Numerical time-series routines that operate on plain arrays. No dates, no
 * database — the catalog `Series` model adapts these to its date-keyed data.
 */
export * from "./disaggregate";
