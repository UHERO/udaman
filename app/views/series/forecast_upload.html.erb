<%= form_with url: { action: :forecast_upload }, method: :post do |f| %>

  <div class="field">
    Forecast <%= f.text_field :forecast_upload, :fcid, nil, size: 8 %> (must be YYQ[1234])
  </div>
  <div class="field">
    Version <%= text_field_tag 'forecast_upload[version]', nil, size: 8 %> (a number or FIN)
  </div>
  <div class="field">
    Upload file <%= file_field_tag 'forecast_upload[forecast_upload_file]', nil %>
  </div>
  <div class="actions">
    <%= f.submit 'Store forecast series' %>
    <%= link_to 'Cancel', :back %>
  </div>

<% end %>
