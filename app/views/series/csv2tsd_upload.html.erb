<%= form_with url: { action: :csv2tsd, format: :tsd }, method: :post do |f| %>

  <div class="field">
    Upload file <%= f.file_field 'csv2tsd[filepath]' %>
  </div>
  <div class="actions">
    <%= f.submit 'Do csv-to-tsd conversion' %>
    <%= link_to 'Cancel', :back %>
  </div>

<% end %>
