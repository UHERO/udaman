<%= form_with url: { action: :csv2tsd_upload }, method: :post do |f| %>

  <div class="field">
    Upload file <%= file_field_tag 'csv2tsd[filepath]' %>
  </div>
  <div class="actions">
    <%= f.submit 'Do conversion' %>
    <%= link_to 'Cancel', :back %>
  </div>

<% end %>
