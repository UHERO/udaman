	<table>
	<tr>
		<td class="label">Units:</td><td><%= @series.units.to_s + '(s)' %></td>
	</tr>
	<tr>
		<td class="label">Data: </td>
		<td><div id="data-points">
			<%= render :partial => 'data_points',
								  :locals => { :series => @series, :as => @as, :chg => @chg, :ytd_chg => @ytd_chg, :lvl_chg => @lvl_chg,
                                loader_count: @series.enabled_data_sources.count }%></div>
		</td>
	</tr>
	</table>
	