<h1>Editing Measurements for <%= @data_list.name %> (<%= @data_list.universe %>)</h1>
<p>
    This screen should only be used for modifying UHERO universe data lists.<br />
    Separate items with any kind of whitespace (no commas, please).
</p>
<%= form_with(url: save_as_text_data_list_path(@data_list), local: true)do |f| %>
<%= text_area_tag(:edit_box, @prefix_list, rows: 30, cols: 30) %>
<p>
    <%= f.submit 'Store list' %>
    <%= link_to 'Cancel', :back %>
</p>
<% end %>